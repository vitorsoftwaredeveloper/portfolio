/* ...existing code... */
// removed large inline script: all application logic moved to modular files:
// removed function initThemeSwitcher() {}
// removed function setLanguage() {}
// removed IndexedDB helper functions and events
// removed portfolio modal and interaction code
// removed custom cursor and hover interactions
// removed login handling
// For backwards-compatibility this file now bootstraps the new modular entry (main.js).
export default {}; // tombstone module (kept for any legacy imports)